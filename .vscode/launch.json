{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "pnpm dev",
            "name": "Development server",
            "request": "launch",
            "cwd": "${workspaceFolder}/web",
            "type": "node-terminal"
        }
    ]
}
